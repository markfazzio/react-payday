// @flow strict

import { MainColors } from './color';

export const Borders = {
    dealCard: {
        inner: `1px solid ${MainColors.black}`,
        outer: `1px solid ${MainColors.lightGray}`,
        image: {
            bottom: `1px solid ${MainColors.black}`,
            top: `1px solid ${MainColors.black}`
        }
    }
};
